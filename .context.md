# .context.md — 会话上下文存档

> 最后更新：2026-06-15
> 用途：新会话读取此文件可恢复讨论上下文

---

## 项目概况

**喝杯水吧** — 喝水记录 Web 应用 + Oppo Watch 2 移植版。

| 组件 | 路径 | 说明 |
|------|------|------|
| Web 版 | `E:\code\glug-water\index.html` | 单页 HTML，localStorage 存储 |
| 手表版 | `E:\code\drink-water-watch\index.html` | Android WebView（Chrome 61） |
| 后端 | Supabase（`iqbkxsnanvupsvzckvrs.supabase.co`） | 排行榜 + 设备配对 + 数据同步 |
| 迁移脚本 | `E:\code\glug-water\migrate-pk-to-user_id.sql` | 已执行 |

---

## 全部功能更新

| # | 内容 | 归属 | 状态 |
|---|------|------|------|
| 1 | 每日喝水记录（增删改） | Web + 手表 | ✅ |
| 2 | 导出/导入数据 | Web | ✅ |
| 3 | 深色模式 | Web | ✅ |
| 4 | 喝水提醒 | Web | ✅ |
| 5 | 排行榜（Supabase，前 20 名） | Web + 手表 | ✅ |
| 6 | 排行榜按需拉取明细（点击头像才拉 `today_records`） | Web | ✅ |
| 7 | 设备配对（手机生成 6 位码，手表输入绑定） | Web + 手表 | ✅ |
| 8 | 管理员模式（排行榜标题连点 5 次进入） | Web | ✅ |
| 9 | 管理员封禁（删除他人排名 + 标记 banned） | Web | ✅ |
| 10 | 调试日志面板（可展开/收起） | Web + 手表 | ✅ |
| 11 | 关于应用页（显示完整 user_id） | Web | ✅ |
| 12 | 喝水历史图表（Chart.js → 已改为 Canvas 自绘） | Web | ✅ |
| 13 | 手表端 Android WebView 封装 | 手表 | ✅ |
| 14 | 同步策略：同步顺序从"先推后拉"改为"拉→合→推" | Web + 手表 | ✅ |
| 15 | 手表端 `sbFetch` 原生 fetch 封装（已有） | 手表 | ✅ |
| 16 | 手机端 `sbFetch` 原生 fetch 封装（本次新增） | Web | ✅ |

---

## 全部已修复的错误

| # | 问题 | 根因 | 修复方式 | 会话 |
|---|------|------|---------|------|
| 1 | **Upsert 返回 409 不写入** | `id` 是主键，`user_id` 仅 UNIQUE，`resolution=merge-duplicates` 用 `id` 检测冲突永不命中 | 执行 SQL 迁移：`user_id` 改为主键，删除 `id` 列 | 本次 |
| 2 | **Safari "无法打开网页，丢失网络连接"** | 页面依赖 CDN（Supabase SDK + Chart.js），Safari 拦截外部 CDN 请求 | 移除 CDN 依赖，全部改用原生 fetch + Canvas 自绘 | 本次 |
| 3 | **手表端先推后拉覆盖手机数据** | 同步顺序是先推本地数据 → 覆盖云端手机数据 → 再拉 | 改为"拉→合→推"，先合并再推送全集 | 此前 |
| 4 | **Web 端展开日志 UI 逻辑问题** | — | 已修复 | 此前 |

---

## 未解决的遗留问题

| 优先级 | 问题 | 归属 | 根因 |
|--------|------|------|------|
| **P0** | 手机端看不到手表推送的数据 | 手机端 | `mergeCloudTodayRecords()` 拉取后返回 0 条 |
| **P1** | 手表端拉不到手机的删除操作 | 手表端 | 合并策略只追加，不删除本地孤立记录 |
| **P1** | 手机端拉不到手表的删除操作 | 手机端 | 同上，方向相反 |

---

## 关键架构与数据

### 数据库结构

**leaderboard 表：**
```sql
user_id TEXT PRIMARY KEY,           -- 手机↔手表共用同一 user_id
nickname TEXT DEFAULT '',
avatar_idx INTEGER DEFAULT 0,
today_ml INTEGER DEFAULT 0,
streak INTEGER DEFAULT 0,
today_records JSONB DEFAULT '[]'::jsonb,  -- [{id, amount, time, mood}]
banned BOOLEAN DEFAULT FALSE,
updated_at TIMESTAMPTZ DEFAULT NOW()
```

**device_pairing 表：**
```sql
pairing_code TEXT PRIMARY KEY,       -- 6 位配对码
user_id TEXT NOT NULL,
created_at TIMESTAMPTZ DEFAULT NOW()
```

### Supabase 凭证
- **URL**: `https://iqbkxsnanvupsvzckvrs.supabase.co`
- **Anon Key**: `sb_publishable_48GJWDTjOtydQXYEPVQZVQ_sFxPvRYf`

### 手机端 user_id
`ad639ebb-62d5-4a21-8d34-9186c335b71d`

---

## 两端同步策略（当前稳定版）

```
手机端 / 手表端

  ① 拉取云端 today_records
      GET /rest/v1/leaderboard?select=today_records&user_id=eq.{uid}&limit=1
  
  ② 按 id 去重合并到本地（仅追加，不删除）
  
  ③ 推送合并后的全集到云端
      POST /rest/v1/leaderboard?on_conflict=user_id
      Prefer: resolution=merge-duplicates
      Body: { user_id, nickname, avatar_idx, today_ml, streak, today_records, updated_at }
```

⚠️ 当前合并策略只追加不删除，这是 P1 问题的根因。

---

## 下一步方向

1. **P0：手机端看不到手表数据** — 排查手机端 `mergeCloudTodayRecords()` 拉取后返回 0 条的原因
2. **P1：删除操作的同步** — 两端合并逻辑改为「以云端为准，删除本地孤立记录」
3. 持续维护此文件

---

## 新会话启动指引

新助手读取此文件后：
1. 先看上面"当前状态"了解进度
2. `E:\code\glug-water\index.html` — 手机端代码（合并逻辑在 `mergeCloudTodayRecords()`）
3. `E:\code\drink-water-watch\index.html` — 手表端代码（合并逻辑在 `syncTodayToLeaderboard()` 第 2 步）
4. 先分析再改代码
